package chess;

import java.util.HashMap;
import java.util.Map;

public class Board {
    private static final Map<String, PieceType> pieceTypes;
    static {
        pieceTypes = new HashMap<>();
    }
}
